import type { LunarDateInfo } from './types';
export declare class LunarDate implements LunarDateInfo {
    day: number;
    month: number;
    year: number;
    leap: boolean;
    jd: number;
    constructor(day: number, month: number, year: number, leap: boolean, jd: number);
    toString(): string;
    isValid(): boolean;
    equals(other: LunarDate): boolean;
}
//# sourceMappingURL=lunar-date.d.ts.map